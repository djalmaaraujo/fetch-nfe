# Núcleo fiscal: comunicação NFeDistribuicaoDFe + assinatura + manifestação.
# A PyNFe 0.6.5 não declara todas as deps no wheel publicado, então pinamos aqui.
pynfe==0.6.5
signxml>=4.1.0
lxml>=5.4.0
pyopenssl>=25.1.0
cryptography>=43.0.3
requests>=2.32

# API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9    # upload do .pfx no cadastro de empresas / XML do DANFE

# Documentos auxiliares (DANFE/DACTE/DAMDFE) a partir do XML autorizado
brazilfiscalreport[dacte,damdfe]>=1.0.2
